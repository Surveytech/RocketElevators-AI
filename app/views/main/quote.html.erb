#quote.html.erb


<%= form_with do |f| %>
	
	<%= f.text_field :line %><br/>
	<%= f.submit %><br/>
	<%= f.collection_select(:buildingtype_id, Buildingtype.all, :id, :type, { :prompt => 'Select a Building Type',:selected => @buildingtype.create}, { class: 'form-control' }) %>

<% end %>